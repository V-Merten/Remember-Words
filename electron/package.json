{
  "name": "remember-words",
  "version": "1.0.2",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build-frontend": "cd ../frontend && npm run build",
    "copy-frontend": "rm -rf ./build && cp -r ../frontend/build ./build",
    "dist": "source .env && npm run build-frontend && npm run copy-frontend && electron-builder --publish always"
  },
  "devDependencies": {
    "electron": "^35.2.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "publish": [
      {
        "provider": "github",
        "owner": "V-Merten",
        "repo": "Remember-Words"
      }
    ],
    "productName": "RememberWords",
    "appId": "com.example.rememberwords",
    "asar": true,
    "asarUnpack": [
      "**/*.jar"
    ],
    "files": [
      "build/**/*",
      "main.js"
    ],
    "extraResources": [
      {
        "from": "java",
        "to": "java"
      },
      {
        "from": "db",
        "to": "db"
      }
    ],
    "directories": {
      "output": "dist"
    }
  },
  "dependencies": {
    "electron-updater": "^6.6.2"
  }
}
